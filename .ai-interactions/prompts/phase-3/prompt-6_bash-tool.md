## `bash` Tool

## Ideation

1. We want to build a `bash` tool that allows LLMs to execute bash commands in the terminal. How would a tool like this work?

## PRD
The next tool we want to build for Keen Code is `bash`